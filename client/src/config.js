export const serverUrl = "https://freshapi.nataliadelgado.dev";
export const apiUrl = `${serverUrl}/api`;
export const imagesURL = `${serverUrl}/images`;
export const sliderURL = `${serverUrl}/slider`;
